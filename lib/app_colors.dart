import 'package:flutter/material.dart';

const Color dgreencolor = Color(0xFF181C14); //Not used.
const Color midgreencolor = Color(0xFF3C3D37); //Not used.
const Color disablecolor = Color(0xFF595959); // Used for disabled state, only pair with beigecolor or white background. Only for UI elements.
const Color lgreencolor = Color(0xFF395639); //Used for encourage color, only pair with white text or white background.
const Color beigecolor = Color(0xFFCBC2B4); //Used for background color, only pair with black text or black background.
const Color redcolor = Color(0xFF991A1A); //Used for destructive, only pair with white text or white background.
const Color bluecolor = Color(0xFF11487A); //Used for links, only pair with white text or white background.
const Color lgreycolor = Color(0xFFe0e0e0); //Used for neutral actions ('Cancel'), only pair with black text or black background.
const Color placeholdercolor = Color(0xFF9e9e9e); //Used for placeholder text, best paired with whitecolor.
const Color blackcolor = Color(0xFF181818); //Used in lieu of regular black for a softer hue. (Safe for above pairings.)
const Color whitecolor = Color(0xFFEEEEEE); //Used in lieu of regular white for a softer hue. (Safe for above pairings.)
